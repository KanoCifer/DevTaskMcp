{
  "mcpServers": {
    "devtask": {
      "command": "uv",
      "args": [
        "run",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "-m",
        "devtask_mcp.server"
      ]
    }
  }
}
